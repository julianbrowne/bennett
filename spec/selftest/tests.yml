#
#   Test Cases
#

---

login_and_logout_user:
   - session.login
   - session.logout

get_basic_details:
   - session.login
   - user.get_user
   - session.logout

basic_user_search:
   - session.login
   - user.search
   - session.logout

update_user_data:
   - session.login
   - user.update
   - session.logout

reset_password:
   - session.password_reset
   - session.password_reset_check
   - session.set_new_password

multi_get:
   - session.login
   - user.get_user
   - user.get_user
   - user.get_user
   - user.get_user

user_fail:
   - test.expect_fail

invalid_session_fail:
   - session.login
   - session.logout
   - user.get_user
